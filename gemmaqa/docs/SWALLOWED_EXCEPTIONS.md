# Silently swallowed exceptions (caught, no log, no re-raise)

62 sites in backend/app. Generated by AST scan.

Each row: the handler's line, the function it sits in, what it catches,
what it does instead, and the first guarded statement.

## agent/controller.py
- **:236** `_clear_credentials()` catches `Exception` -> `pass`
  - guards: `self.request.password = None`
- **:290** `_record_span()` catches `Exception` -> `pass`
  - guards: `elapsed_ms = (time.monotonic() - started) * 1000.0`
- **:519** `_observe()` catches `Exception` -> `pass`
  - guards: `await adapter.wait(reobservation_interval_ms(observation_pass))`
- **:1059** `run()` catches `Exception` -> `pass`
  - guards: `await adapter.navigate(self.request.url)`
- **:1412** `run()` catches `Exception` -> `pass`
  - guards: `evidence.record_trace(browser.trace_path)`
- **:2060** `_run_cleanup_pass()` catches `Exception` -> `pass`
  - guards: `await adapter.wait(reobservation_interval_ms(reobservations))`
- **:2086** `_run_cleanup_pass()` catches `Exception` -> `pass`
  - guards: `await adapter.wait(reobservation_interval_ms(reobservations))`

## agent/form_workflow.py
- **:708** `replan_conservatively()` catches `Exception` -> `continue`
  - guards: `replacement = generate_conservative_value(`

## agent/memory.py
- **:1118** `record_submission_outcome()` catches `Exception` -> `return`
  - guards: `entry = outcome.to_dict()`
- **:1321** `memory_snapshot()` catches `Exception` -> `pass`
  - guards: `snap["entities"] = {`
- **:1330** `memory_snapshot()` catches `Exception` -> `pass`
  - guards: `snap["actors"] = {`
- **:1340** `memory_snapshot()` catches `Exception` -> `pass`
  - guards: `snap["workflows"] = {`
- **:1350** `memory_snapshot()` catches `Exception` -> `pass`
  - guards: `snap["dependencies"] = {`
- **:1371** `memory_snapshot()` catches `Exception` -> `pass`
  - guards: `stats = self.knowledge_graph.statistics()`
- **:1388** `memory_snapshot()` catches `Exception` -> `pass`
  - guards: `stats = self.goal_engine.statistics()`
- **:1410** `memory_snapshot()` catches `Exception` -> `pass`
  - guards: `stats = self.scenario_engine.statistics()`
- **:1428** `memory_snapshot()` catches `Exception` -> `pass`
  - guards: `stats = self.strategy_engine.statistics()`
- **:1446** `memory_snapshot()` catches `Exception` -> `pass`
  - guards: `stats = self.investigation_engine.statistics()`
- **:1469** `memory_snapshot()` catches `Exception` -> `pass`
  - guards: `stats = self.understanding_engine.statistics()`

## agent/run_manager.py
- **:168** `_runner()` catches `Exception` -> `pass`
  - guards: `start_request.password = None`

## api/websocket.py
- **:77** `enqueue()` catches `asyncio.QueueEmpty` -> `pass`
  - guards: `self.queue.get_nowait()`
- **:104** `disconnect()` catches `asyncio.QueueFull` -> `pass`
  - guards: `client.queue.put_nowait(None)`
- **:121** `drop_run()` catches `asyncio.QueueFull` -> `pass`
  - guards: `client.queue.put_nowait(None)`
- **:259** `run_progress_ws()` catches `WebSocketDisconnect` -> `pass`
  - guards: `await websocket.send_json(`
- **:254** `run_progress_ws()` catches `asyncio.TimeoutError` -> `continue`
  - guards: `raw = await asyncio.wait_for(websocket.receive_text(), timeout=6`
- **:268** `run_progress_ws()` catches `asyncio.CancelledError` -> `pass`
  - guards: `await sender`

## browser/adapters/direct.py
- **:155** `wait_for_page_stable()` catches `Exception` -> `pass`
  - guards: `await page.wait_for_load_state("domcontentloaded", timeout=timeo`
- **:220** `_locator_for()` catches `Exception` -> `continue`
  - guards: `if await first.count() > 0:`

## browser/adapters/mcp.py
- **:209** `get_current_url()` catches `Exception` -> `pass`
  - guards: `result = await self._call("browser_evaluate", {"function": "() =`
- **:220** `get_page_title()` catches `Exception` -> `pass`
  - guards: `result = await self._call("browser_evaluate", {"function": "() =`
- **:257** `get_console_events()` catches `Exception` -> `pass`
  - guards: `result = await self._call("browser_console_messages", {})`
- **:268** `get_network_events()` catches `Exception` -> `pass`
  - guards: `result = await self._call("browser_network_requests", {})`
- **:285** `list_tabs()` catches `Exception` -> `pass`
  - guards: `result = await self._call("browser_tabs", {"action": "list"})`
- **:719** `_rpc()` catches `json.JSONDecodeError` -> `continue`
  - guards: `data = json.loads(line.decode("utf-8"))`

## browser/evidence.py
- **:96** `screenshot()` catches `OSError` -> `pass`
  - guards: `self._bytes_used += path.stat().st_size`
- **:179** `screenshot_via_adapter()` catches `OSError` -> `pass`
  - guards: `self._bytes_used += path.stat().st_size`

## browser/executor.py
- **:286** `_execute_via_adapter()` catches `Exception` -> `pass`
  - guards: `after_url = await adapter.get_current_url()`
- **:353** `_execute_via_adapter()` catches `Exception` -> `pass`
  - guards: `title = await adapter.get_page_title()`
- **:582** `_execute_via_page()` catches `Exception` -> `pass`
  - guards: `meta = self.evidence.record_metadata(`
- **:650** `_resolve_element()` catches `TypeError` -> `pass`
  - guards: `if await locator.count() > 0:`
- **:684** `_resolve_semantic()` catches `Exception` -> `continue`
  - guards: `if await first.count() > 0:`
- **:714** `_semantic_fallback()` catches `Exception` -> `continue`
  - guards: `loc = builder()`
- **:772** `_wait_settled()` catches `Exception` -> `pass`
  - guards: `wait_kind = "networkidle" if extra_ms else "domcontentloaded"`

## browser/locators.py
- **:247** `_build_locator()` catches `Exception` -> `continue`
  - guards: `return page.locator(fb).first`

## browser/manager.py
- **:178** `login()` catches `Exception` -> `pass`
  - guards: `await page.wait_for_load_state("networkidle", timeout=5000)`
- **:201** `login()` catches `Exception` -> `pass`
  - guards: `shot = await evidence.screenshot(page, "login_failed")`
- **:286** `stop()` catches `Exception` -> `pass`
  - guards: `await self.context.tracing.stop()`

## browser/network_monitor.py
- **:77** `on_request()` catches `Exception` -> `pass`
  - guards: `self._request_start[request.url] = datetime.utcnow().timestamp()`

## browser/observer.py
- **:506** `observe()` catches `Exception` -> `pass`
  - guards: `await page.wait_for_load_state("domcontentloaded", timeout=3000)`
- **:527** `observe_adapter()` catches `Exception` -> `pass`
  - guards: `url = url or await adapter.get_current_url()`
- **:532** `observe_adapter()` catches `Exception` -> `pass`
  - guards: `title = title or await adapter.get_page_title()`
- **:548** `observe_adapter()` catches `Exception` -> `pass`
  - guards: `console_events = await adapter.get_console_events()`

## gemma/model_context.py
- **:303** `_as_dict()` catches `Exception` -> `pass`
  - guards: `result = fn()`

## intelligence/autonomous_investigation/autonomous_investigation_engine.py
- **:404** `_record_immediate_outcome()` catches `Exception` -> `pass`
  - guards: `scenario.status = "blocked" if outcome == "blocked" else outcome`

## intelligence/knowledge_graph/graph_context_projector.py
- **:253** `project_model_context()` catches `Exception` -> `continue`
  - guards: `slice_ = active_projector.context_for_node(node_id, max_neighbou`
- **:278** `project_model_context()` catches `Exception` -> `pass`
  - guards: `stats = context.knowledge_graph.statistics()`

## intelligence/scenario_planning/scenario_actor_resolver.py
- **:59** `_derive_actor_terms_from_graph()` catches `Exception` -> `continue`
  - guards: `steps = graph.query_engine.workflow_steps(workflow_node_id)`

## perception/dom_extractor.py
- **:903** `extract_raw()` catches `Exception` -> `pass`
  - guards: `await page.wait_for_load_state("domcontentloaded", timeout=3000)`

## reporting/activity_log.py
- **:379** `iter_records()` catches `OSError` -> `return`
  - guards: `with path.open("r", encoding="utf-8") as handle:`
- **:375** `iter_records()` catches `json.JSONDecodeError` -> `continue`
  - guards: `yield json.loads(line)`

## reporting/service.py
- **:39** `load_report_model()` catches `Exception` -> `pass`
  - guards: `return FinalReport.model_validate(json.loads(report_json))`
- **:48** `load_report_model()` catches `Exception` -> `continue`
  - guards: `return FinalReport.model_validate(json.loads(path.read_text(enco`
