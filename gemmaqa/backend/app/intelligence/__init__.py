"""Application intelligence — cross-run/cross-page understanding built ON TOP
of the deterministic observation layers (Perception Engine, ApplicationStore),
never inside them. Everything here consumes already-observed, already-typed
evidence; nothing here touches the browser."""
