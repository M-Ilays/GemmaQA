fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
sqlalchemy[asyncio]>=2.0.36
aiosqlite>=0.20.0
playwright>=1.48.0
httpx>=0.27.0
Pillow>=10.4.0
python-dotenv>=1.0.1
pytest>=8.0.0
pytest-asyncio>=0.24.0

# Optional provider backends are NOT pinned here — install the one you use:
#   GEMMA_PROVIDER=bedrock       pip install -r backend/requirements-bedrock.txt
#   AWS Strands coordinator      pip install -r backend/requirements-strands.txt
#   GEMMA_PROVIDER=transformers  install torch + transformers yourself
# Each provider imports its SDK lazily, so a mock or openai_compatible install
# stays lean and needs none of them.
